#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
# デフォルトモデル
MODEL_NAME="llama3"

source "${ROOT_DIR}/.venv/bin/activate"
python3 "${ROOT_DIR}/main.py" --backend ollama --model "${MODEL_NAME}" "$@"
