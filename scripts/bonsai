#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
MODEL_NAME="prism-ml/Bonsai-8B-mlx-1bit"

source "${ROOT_DIR}/.venv/bin/activate"
python3 "${ROOT_DIR}/main.py" --backend bonsai --model "${MODEL_NAME}" "$@"
