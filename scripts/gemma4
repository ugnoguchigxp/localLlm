#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
MODEL_NAME="mlx-community/gemma-4-e4b-it-4bit"

source "${ROOT_DIR}/.venv/bin/activate"
python3 "${ROOT_DIR}/main.py" --backend mlx --model "${MODEL_NAME}" "$@"
